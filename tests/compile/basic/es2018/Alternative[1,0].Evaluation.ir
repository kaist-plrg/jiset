{
  access __x0__ = (Alternative "Evaluation" direction)
  let m1 = __x0__
  access __x1__ = (Term "Evaluation" direction)
  let m2 = __x1__
  if (= direction 1i) ??? "Return an internal Matcher closure that takes two arguments , a State id:{x} and a Continuation id:{c} , and performs the following steps when evaluated : in:{} out:{}" else {
    assert (= direction -1i)
    ??? "Return an internal Matcher closure that takes two arguments , a State id:{x} and a Continuation id:{c} , and performs the following steps when evaluated : in:{} out:{}"
  }
}