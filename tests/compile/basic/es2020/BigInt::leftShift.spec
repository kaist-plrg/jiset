            1. If _y_ < *0n*, then
              1. Return the BigInt value that represents _x_ ÷ 2<sup>-_y_</sup>, rounding down to the nearest integer, including for negative numbers.
            1. Return the BigInt value that represents _x_ × 2<sup>_y_</sup>.