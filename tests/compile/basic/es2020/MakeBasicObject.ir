{
  ??? "Let id:{obj} be a newly created object with an internal slot for each name in id:{internalSlotsList} ."
  if (contains internalSlotsList "Extensible") obj.Extensible = true else {}
  app __x0__ = (WrapCompletion obj)
  return __x0__
}