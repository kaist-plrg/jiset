{
  ??? "value:{+0} if value:{+0} ≤ DayWithinYear ( id:{t} ) < value:{31}"
  ??? "value:{1} if value:{31} ≤ DayWithinYear ( id:{t} ) < value:{59} + InLeapYear ( id:{t} )"
  ??? "value:{2} if value:{59} + InLeapYear ( id:{t} ) ≤ DayWithinYear ( id:{t} ) < value:{90} + InLeapYear ( id:{t} )"
  ??? "value:{3} if value:{90} + InLeapYear ( id:{t} ) ≤ DayWithinYear ( id:{t} ) < value:{120} + InLeapYear ( id:{t} )"
  ??? "value:{4} if value:{120} + InLeapYear ( id:{t} ) ≤ DayWithinYear ( id:{t} ) < value:{151} + InLeapYear ( id:{t} )"
  ??? "value:{5} if value:{151} + InLeapYear ( id:{t} ) ≤ DayWithinYear ( id:{t} ) < value:{181} + InLeapYear ( id:{t} )"
  ??? "value:{6} if value:{181} + InLeapYear ( id:{t} ) ≤ DayWithinYear ( id:{t} ) < value:{212} + InLeapYear ( id:{t} )"
  ??? "value:{7} if value:{212} + InLeapYear ( id:{t} ) ≤ DayWithinYear ( id:{t} ) < value:{243} + InLeapYear ( id:{t} )"
  ??? "value:{8} if value:{243} + InLeapYear ( id:{t} ) ≤ DayWithinYear ( id:{t} ) < value:{273} + InLeapYear ( id:{t} )"
  ??? "value:{9} if value:{273} + InLeapYear ( id:{t} ) ≤ DayWithinYear ( id:{t} ) < value:{304} + InLeapYear ( id:{t} )"
  ??? "value:{10} if value:{304} + InLeapYear ( id:{t} ) ≤ DayWithinYear ( id:{t} ) < value:{334} + InLeapYear ( id:{t} )"
  ??? "value:{11} if value:{334} + InLeapYear ( id:{t} ) ≤ DayWithinYear ( id:{t} ) < value:{365} + InLeapYear ( id:{t} )"
}