{
  if (< y 0i) ??? "Return the BigInt value that represents ℝ ( id:{x} ) ÷ 2 sup:{- id:{y} } , rounding down to the nearest integer , including for negative numbers ." else {}
  app __x0__ = (WrapCompletion (convert (* x (** 2.0 y)) num2bigint ))
  return __x0__
}