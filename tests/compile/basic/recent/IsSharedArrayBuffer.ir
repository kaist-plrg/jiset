{
  let bufferData = obj.ArrayBufferData
  if (= bufferData null) return false else {}
  if (is-instance-of bufferData DataBlock) return false else {}
  assert (is-instance-of bufferData SharedDataBlock)
  return true
}