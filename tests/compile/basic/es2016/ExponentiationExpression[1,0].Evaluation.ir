{
  access __x0__ = (UpdateExpression "Evaluation")
  let left = __x0__
  app __x1__ = (GetValue left)
  let leftValue = [? __x1__]
  access __x2__ = (ExponentiationExpression "Evaluation")
  let right = __x2__
  app __x3__ = (GetValue right)
  let rightValue = [? __x3__]
  app __x4__ = (ToNumber leftValue)
  let base = [? __x4__]
  app __x5__ = (ToNumber rightValue)
  let exponent = [? __x5__]
  ??? "Return the result of < emu - xref href = \" # sec - applying - the - exp - operator \" title > Applying the * * operator < / emu - xref > with id:{base} and id:{exponent} as specified in link:{unhandled: sec-applying-the-exp-operator} ."
}