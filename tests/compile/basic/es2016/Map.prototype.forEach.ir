{
  let M = this
  if (! (= (typeof M) Object)) throw TypeError else {}
  if (= M.MapData absent) throw TypeError else {}
  app __x0__ = (IsCallable callbackfn)
  if (= __x0__ false) throw TypeError else {}
  if (! (= thisArg absent)) let T = thisArg else let T = undefined
  let entries = M.MapData
  ??? "Repeat for each Record { [ [ Key ] ] , [ [ Value ] ] } id:{e} that is an element of id:{entries} , in original key insertion order in:{} out:{}"
  return undefined
}