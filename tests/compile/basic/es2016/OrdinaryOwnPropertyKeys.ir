{
  let keys = (new [])
  ??? "For each own property key id:{P} of id:{O} that is an integer index , in ascending numeric index order in:{} out:{}"
  ??? "For each own property key id:{P} of id:{O} that is a String but is not an integer index , in ascending chronological order of property creation in:{} out:{}"
  ??? "For each own property key id:{P} of id:{O} that is a Symbol , in ascending chronological order of property creation in:{} out:{}"
  return keys
}