{
  assert (= generator.GeneratorState undefined)
  let genContext = GLOBAL_context
  genContext.Generator = generator
  ??? "Set the code evaluation state of id:{genContext} such that when evaluation is resumed for that execution context the following steps will be performed : step-list:{...}"
  generator.GeneratorContext = genContext
  generator.GeneratorState = "suspendedStart"
  app __x0__ = (NormalCompletion undefined)
  app __x1__ = (WrapCompletion __x0__)
  return __x1__
}