{
  let bufferData = obj.ArrayBufferData
  if (= bufferData null) return false else {}
  if (= (typeof bufferData) "DataBlock") return false else {}
  assert (= (typeof bufferData) "SharedDataBlock")
  return true
}