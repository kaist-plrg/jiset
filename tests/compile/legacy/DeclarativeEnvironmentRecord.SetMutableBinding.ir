{
  let envRec = this
  ??? "If id:{envRec} does not have a binding for id:{N} , then in:{} out:{}"
  ??? "If the binding for id:{N} in id:{envRec} is a strict binding , set id:{S} to value:{true} ."
  ??? "If the binding for id:{N} in id:{envRec} has not yet been initialized , throw a value:{ReferenceError} exception ."
  ??? "Else if the binding for id:{N} in id:{envRec} is a mutable binding , change its bound value to id:{V} ."
  ??? "Else , in:{} out:{}"
  app __x0__ = (NormalCompletion CONST_empty)
  return __x0__
}