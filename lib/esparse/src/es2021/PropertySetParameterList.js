const Node = require('../Node');

let PropertySetParameterList = (Yield, Await) => (given) => {
  Yield, Await, given;
  Node.TODO('PropertySetParameterList');
}

module.exports = PropertySetParameterList;
