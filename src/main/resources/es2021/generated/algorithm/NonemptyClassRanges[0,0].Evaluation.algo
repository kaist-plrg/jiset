def [SYNTAX] NonemptyClassRanges[0,0].Evaluation[R]<(ClassAtom),true>() = 0:{
  0:access __x0__ = (ClassAtom "Evaluation")
  0:return __x0__
}
- id: "sec-nonemptyclassranges"
- code:
          1. Return the CharSet that is the result of evaluating |ClassAtom|.