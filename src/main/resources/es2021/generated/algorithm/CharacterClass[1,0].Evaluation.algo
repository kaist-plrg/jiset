def [SYNTAX] CharacterClass[1,0].Evaluation[R]<(ClassRanges),true>() = 0:{
  0:access __x0__ = (ClassRanges "Evaluation")
  0:let A = __x0__
  1:return (new [A, true])
}
- id: "sec-characterclass"
- code:
          1. Evaluate |ClassRanges| to obtain a CharSet _A_.
          1. Return the two results _A_ and *true*.