def [SYNTAX] IdentifierPart[2,0].EarlyErrors[S]<(UnicodeEscapeSequence),true>() = 0:??? "It is a Syntax Error if the SV of nt:{UnicodeEscapeSequence} is none of value:{\"$\"} , value:{\"_\"} , ! UTF16EncodeCodePoint ( < ZWNJ > ) , ! UTF16EncodeCodePoint ( < ZWJ > ) , or ! UTF16EncodeCodePoint ( id:{cp} ) for some Unicode code point id:{cp} that would be matched by the nt:{UnicodeIDContinue} lexical grammar production ."
- id: "sec-identifier-names-static-semantics-early-errors"
- code:
          <li>
            It is a Syntax Error if the SV of |UnicodeEscapeSequence| is none of *"$"*, *"_"*, ! UTF16EncodeCodePoint(<ZWNJ>), ! UTF16EncodeCodePoint(<ZWJ>), or ! UTF16EncodeCodePoint(_cp_) for some Unicode code point _cp_ that would be matched by the |UnicodeIDContinue| lexical grammar production.
          </li>