def [SYNTAX] QuantifierPrefix[5,0].EarlyErrors<`{` DecimalDigits `,` DecimalDigits `}`>() = {
  0:access __x0__ = (DecimalDigits0 "MV")
  0:access __x1__ = (DecimalDigits1 "MV")
  0:if (< __x1__ __x0__) throw SyntaxError else 2:{}
}
- id: "sec-patterns-static-semantics-early-errors"
- code:
          <li>
            It is a Syntax Error if the MV of the first |DecimalDigits| is larger than the MV of the second |DecimalDigits|.
          </li>