def YearFromTime(t) = 0:??? "YearFromTime ( id:{t} ) = the largest integral Number id:{y} ( closest to + ∞ ) such that TimeFromYear ( id:{y} ) ≤ id:{t}"
- id: "eqn-YearFromTime"
- code:
YearFromTime(_t_) = the largest integral Number _y_ (closest to +∞) such that TimeFromYear(_y_) ≤ _t_