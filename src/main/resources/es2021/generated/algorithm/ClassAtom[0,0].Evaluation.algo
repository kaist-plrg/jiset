def [SYNTAX] ClassAtom[0,0].Evaluation[R]<(),true>() = 0:??? "Return the CharSet containing the single character code:{-} U + 002D ( HYPHEN - MINUS ) ."
- id: "sec-classatom"
- code:
          1. Return the CharSet containing the single character `-` U+002D (HYPHEN-MINUS).