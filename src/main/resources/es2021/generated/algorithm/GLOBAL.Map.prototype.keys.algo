def [BUILTIN] Map.prototype.keys() = 0:{
  0:let M = this
  1:app __x0__ = (CreateMapIterator M ~key~)
  1:return [? __x0__]
}
- id: "sec-map.prototype.keys"
- code:
          1. Let _M_ be the *this* value.
          1. Return ? CreateMapIterator(_M_, ~key~).