def [SYNTAX] ClassRanges[1,0].Evaluation[R]<(NonemptyClassRanges),true>() = 0:{
  0:access __x0__ = (NonemptyClassRanges "Evaluation")
  0:return __x0__
}
- id: "sec-classranges"
- code:
          1. Return the CharSet that is the result of evaluating |NonemptyClassRanges|.