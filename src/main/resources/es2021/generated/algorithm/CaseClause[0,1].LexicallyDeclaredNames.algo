def [SYNTAX] CaseClause[0,1].LexicallyDeclaredNames[S]<(Expression, StatementList),true>() = 0:{
  0:if (! (= StatementList absent)) 0:{
    0:access __x0__ = (StatementList "LexicallyDeclaredNames")
    0:return __x0__
  } else 0:{}
  1:return (new [])
}
- id: "sec-static-semantics-lexicallydeclarednames"
- code:
        1. If the |StatementList| is present, return the LexicallyDeclaredNames of |StatementList|.
        1. Return a new empty List.