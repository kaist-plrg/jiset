def HourFromTime(t) = 0:{
  0:app __x0__ = (floor (/ t 3600000.0))
  0:return (%% __x0__ HoursPerDay)
}
- id: "eqn-HourFromTime"
- code:
HourFromTime(_t_) = 𝔽(floor(ℝ(_t_ / msPerHour)) modulo HoursPerDay)