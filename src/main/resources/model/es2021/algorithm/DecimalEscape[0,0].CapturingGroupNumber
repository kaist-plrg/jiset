def [SYNTAX] DecimalEscape[0,0].CapturingGroupNumber<NonZeroDigit>() = {
  0:access __x0__ = (NonZeroDigit "MV")
  0:return __x0__
}
- id: sec-patterns-static-semantics-capturing-group-number
- code:
          1. Return the MV of |NonZeroDigit|.