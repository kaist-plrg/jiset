def [SYNTAX] ClassAtomNoDash[0,0].Evaluation<SourceCharacter but not `\` or `]` or `-`>() = return (new [SourceCharacter])
- id: "sec-classatomnodash"
- code:
          1. Return the CharSet containing the character matched by |SourceCharacter|.