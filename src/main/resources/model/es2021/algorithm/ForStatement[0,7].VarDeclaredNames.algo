def [SYNTAX] ForStatement[0,7].VarDeclaredNames<`for` `(` Expression? `;` Expression? `;` Expression? `)` Statement>() = {
  0:access __x0__ = (Statement "VarDeclaredNames")
  0:return __x0__
}
- id: "sec-static-semantics-vardeclarednames"
- code:
        1. Return the VarDeclaredNames of |Statement|.